import { BackendDashboard } from "@/components/backend/backend-dashboard";

export const metadata = {
  title: "Backend Dashboard | Jeroen & Paws",
  description: "A polished backend dashboard concept for managing Jeroen & Paws bookings, clients, dogs, services, and revenue.",
};

export default function BackendPage() {
  return <BackendDashboard />;
}